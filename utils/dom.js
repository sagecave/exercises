// document.querySelectorAll() forkortelse
export function $(element) {
  return document.querySelector(element);
}

// document.querySelector() forkortelse
export function $$(element) {
  return document.querySelectorAll(element);
}
